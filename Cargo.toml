[package]
name = "agdk_egui"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
# Tambahkan ini agar tipe AndroidApp jelas
android-activity = { version = "0.5", features = ["native-activity"] }
eframe = { version = "0.27", default-features = false, features = ["android-native-activity", "wgpu"] }
